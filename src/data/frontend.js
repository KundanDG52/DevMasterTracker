// Frontend - 6 topics.
// Shape: { key, cat, name, icon, hue, docs, blurb,
//          sections:[{ t, s, d, k:[] }], detail:{ "Skill": { def,dia,code,use,adv,dis,co,alt,vid,iq } } }
export const frontend = [
{ key: "react", cat: "Frontend", name: "React", icon: "RX", hue: "#61dafb", docs: "https://react.dev/learn",
    blurb: "React is a declarative library for building UIs from composable components. You describe what the UI should look like for any given state, and React efficiently updates the DOM when that state changes via a virtual DOM and reconciliation. Hooks give function components state and side effects, making React the most widely-used frontend tool in the world.",
    sections: [
      { t: "Core Fundamentals", s: "Components, JSX, props, state", d: "2-3 wks", k: ["JSX and rendering model", "Components and composition", "Props and one-way flow", "useState basics", "Event handling", "Conditional rendering", "Lists and keys"] },
      { t: "Hooks Deep Dive", s: "State and side effects", d: "2-3 wks", k: ["useEffect and lifecycle", "useRef", "useContext", "useReducer", "useMemo and useCallback", "Custom hooks", "Rules of hooks"] },
      { t: "Patterns and Performance", s: "Scaling components", d: "2-3 wks", k: ["Lifting state up", "Composition vs inheritance", "Memoization and re-renders", "Code splitting and lazy", "Error boundaries", "Refs forwarding"] },
      { t: "Ecosystem", s: "Routing, data, state", d: "1 mo", k: ["React Router", "Data fetching (TanStack Query)", "Global state (Redux/Zustand)", "Forms (React Hook Form)", "Styling (Tailwind)", "Testing (RTL)"] },
    ],
    detail: { "JSX and rendering model": {
      def: "JSX is a syntax extension that lets you write HTML-like markup directly inside JavaScript; a build tool transpiles it into React.createElement calls that produce a tree of lightweight JS objects called the virtual DOM. When state changes, React re-runs your component to get a new tree, diffs it against the previous one (reconciliation), and applies only the minimal real-DOM mutations needed. This declarative model is what makes React predictable and fast.",
      dia: "You write JSX\n   | Babel/SWC transpiles\n   v\nReact.createElement(...)\n   | produces\n   v\nVirtual DOM (JS object tree)\n   | state changes -> diff (reconcile)\n   v\nMinimal Real DOM updates (browser)",
      code: "function App() {\n  const name = \"Bittu\";\n  return (\n    <div className=\"card\">\n      <h1>Hello, {name}!</h1>\n      <p>Welcome to React</p>\n    </div>\n  );\n}\n// compiles to React.createElement('div', ...)",
      use: "Every React UI: dashboards, e-commerce, social feeds, design tools, at Meta, Netflix, Airbnb and beyond.",
      adv: ["HTML-like syntax is approachable", "Full JS power inside markup", "Declarative, predictable updates", "Virtual DOM minimises DOM work"],
      dis: ["Requires a build step", "Mixes markup and logic at first", "JSX errors can be cryptic", "Re-render pitfalls without memo"],
      co: "Meta (creator), Netflix, Airbnb, Vercel, Atlassian: core products built on JSX.",
      alt: "Vue templates, Angular templates, Svelte; or React without JSX using createElement directly.",
      vid: [{ t: "React tutorial for beginners", q: "react tutorial for beginners" }, { t: "JSX explained", q: "react jsx explained" }, { t: "React in 100 seconds", q: "react js in 100 seconds" }] , iq: [{"q":"What does JSX compile to and why does it matter?","a":"It compiles to React.createElement / the automatic runtime jsx() calls, producing plain JS objects (the virtual DOM). It matters because JSX is JavaScript, not HTML, so you can embed expressions and you need a build step."},{"q":"What is reconciliation?","a":"On a state change React builds a new virtual DOM tree and diffs it against the previous one to compute the minimal real-DOM mutations. Keys let it match list items across renders."},{"q":"Why must list items have stable keys?","a":"Keys identify which items changed, were added or removed. Using the array index breaks on reorder and causes wrong DOM/state reuse. Use a stable unique id."}] },
    "useEffect and lifecycle": {
      def: "useEffect lets function components run side effects (data fetching, subscriptions, timers, manual DOM work) after render. You pass a function and a dependency array: an empty array runs once on mount, [a, b] re-runs when a or b change, and omitting it runs after every render. Returning a cleanup function tears down subscriptions before the next run or on unmount. Understanding the dependency array is the key to avoiding stale closures and infinite loops.",
      dia: "render -> paint -> useEffect runs\n                    |\n deps changed? - yes -> cleanup() then effect()\n              - no  -> skip\n unmount -> cleanup()\n\n []    -> run once (mount)\n [a,b] -> run when a or b change\n (none)-> run after every render",
      code: "useEffect(() => {\n  const id = setInterval(() => tick(), 1000);\n  return () => clearInterval(id); // cleanup\n}, []); // run once on mount\n\nuseEffect(() => {\n  fetchUser(userId).then(setUser);\n}, [userId]); // re-run when userId changes",
      use: "Fetching data on mount, subscribing to events/websockets, syncing with non-React systems, timers.",
      adv: ["One API for all side effects", "Cleanup prevents leaks", "Dependency array gives control", "Composable in custom hooks"],
      dis: ["Stale closures with wrong deps", "Infinite loops from bad deps", "Runs after paint (timing surprises)", "Fires twice in StrictMode"],
      co: "Used in essentially every React app for data loading and subscriptions.",
      alt: "Data libraries (TanStack Query, SWR) replace many manual effects; useLayoutEffect for sync DOM reads.",
      vid: [{ t: "useEffect explained", q: "react useeffect hook explained" }, { t: "useEffect deep dive", q: "react useeffect dependency array deep dive" }, { t: "React hooks course", q: "react hooks full course" }] , iq: [{"q":"When does useEffect run and how does the dependency array control it?","a":"It runs after paint. An empty array runs once on mount; [a,b] re-runs when a or b change; no array runs after every render. The returned cleanup runs before the next effect and on unmount."},{"q":"What causes an infinite useEffect loop?","a":"Updating state the effect depends on without correct deps, or recreating an object/function dependency each render. Fix with correct deps, useCallback/useMemo, or a functional state update."},{"q":"useEffect vs useLayoutEffect?","a":"useEffect runs asynchronously after paint; useLayoutEffect runs synchronously after DOM mutation but before paint. Use the latter only when you must measure layout to avoid a visible flicker."}] } } },
{ key: "react_adv", cat: "Frontend", name: "Advanced React", icon: "RX+", hue: "#22d3ee", docs: "https://react.dev/reference/react",
    blurb: "Advanced React is what comes after you are comfortable with components and hooks: precisely controlling re-renders, designing reusable hook and component APIs, choosing the right state architecture, and using the concurrent features (transitions, Suspense, Server Components, Actions). This is the level interviews probe for senior frontend roles.",
    sections: [
      { t: "Rendering & Performance", s: "Control what re-renders and when", d: "2-3 wks", k: ["How React re-renders (reference equality)", "React.memo (and when it does nothing)", "useMemo & useCallback (when they help)", "Reconciliation & keys deep dive", "List virtualization (windowing)", "Profiler & measuring renders", "Avoiding context re-render storms"] },
      { t: "Advanced Hooks & Patterns", s: "Reusable, composable APIs", d: "2-3 wks", k: ["Custom hook design & rules", "useRef patterns & useImperativeHandle", "useLayoutEffect vs useEffect", "useReducer + Context architecture", "useSyncExternalStore", "Compound components", "Render props & HOCs vs hooks"] },
      { t: "State & Data Architecture", s: "Client state vs server state", d: "2-3 wks", k: ["State colocation & lifting correctly", "Server state with TanStack Query", "Client state (Zustand / Jotai / Redux Toolkit)", "Normalized state", "Optimistic updates", "Derived state & selectors", "Caching & invalidation strategy"] },
      { t: "Concurrent & Modern React", s: "React 18 / 19 features", d: "2-3 wks", k: ["Concurrent rendering model", "useTransition & useDeferredValue", "Suspense for data & code", "Streaming SSR & hydration", "React Server Components", "Actions, useActionState & useOptimistic", "Error boundaries"] },
      { t: "Quality & Tooling", s: "Test, split, ship", d: "1-2 wks", k: ["React Testing Library (advanced)", "Testing hooks & async UI", "Code splitting & lazy boundaries", "Bundle analysis", "Accessibility (a11y) in depth", "Strict Mode & double invoke", "Profiling production builds"] },
    ],
    detail: {
      "useMemo & useCallback (when they help)": {
        def: "useMemo caches the result of a calculation and useCallback caches a function's identity between renders, each keyed by a dependency array. They exist to preserve reference equality - so a memoized child (React.memo) can skip rendering, and an expensive computation is not repeated. The advanced insight most people miss: they are not free (memory plus a deps comparison every render) and they do nothing useful unless something downstream actually depends on a stable reference or the computation is genuinely heavy.",
        dia: "parent re-renders\n   |  recreates inline {} and (() => {})\n   v  new reference every render\nReact.memo(Child) shallow-compares props\n   |  refs differ -> Child re-renders anyway\n   v\nuseMemo / useCallback keep the ref stable\n   -> memo compare passes -> Child skips render",
        code: "// new function each render -> memoized child still re-renders\nconst onPick = (id) => select(id);\n// stable identity while deps unchanged\nconst onPick = useCallback((id) => select(id), [select]);\n// heavy compute, only when rows change\nconst sorted = useMemo(() => rows.slice().sort(cmp), [rows]);\n// pointless: cheap value, no memoized consumer -> pure overhead\nconst x = useMemo(() => a + b, [a, b]);",
        use: "Used on hot paths: large lists, expensive derived data, and props passed to React.memo children or to other hooks' dependency arrays - after the Profiler shows a real re-render cost.",
        adv: ["Preserves reference equality so React.memo children can skip renders", "Avoids repeating genuinely expensive calculations", "Stabilizes a value/function used in another hook's deps", "Makes render cost predictable on heavy screens"],
        dis: ["Costs memory plus a deps comparison on every render", "Does nothing if no memoized consumer depends on the reference", "Wrong or missing deps cause stale values and subtle bugs", "Over-memoizing adds noise and can be net slower"],
        co: "Standard knowledge on senior frontend teams; the React Compiler is starting to automate it, but understanding why it works is still expected in reviews and interviews.",
        alt: "The React Compiler auto-memoizes at build time; otherwise restructure props/state so fewer things change, or split components, before reaching for manual memoization.",
        vid: [{ t: "useMemo & useCallback explained", q: "react usememo usecallback explained when to use" }, { t: "React re-render performance", q: "react rerender performance optimization" }, { t: "React Compiler overview", q: "react compiler explained" }],
        iq: [
          { q: "Does wrapping every function in useCallback improve performance?", a: "No. It only helps when that function is a dependency of a memoized child (React.memo) or another hook. Otherwise you pay the deps comparison and memory cost for zero benefit and add noise - measure with the Profiler first." },
          { q: "Why might a React.memo component still re-render?", a: "Because a prop's reference changes every render - an inline object/array/function or a fresh context value. memo does a shallow compare, so unstable references defeat it; stabilize them with useMemo/useCallback or restructure the props." },
          { q: "What is replacing manual memoization?", a: "The React Compiler memoizes components and values automatically at build time, so you write plain code and it inserts the equivalent where safe. The mental model still matters, and manual memoization is still needed where the compiler is not used." },
        ],
      },
      "useTransition & useDeferredValue": {
        def: "These are React 18 concurrent APIs for keeping the UI responsive during expensive updates. useTransition marks a state update as non-urgent (a transition) so React can interrupt it to handle urgent updates like typing, and gives you an isPending flag for feedback. useDeferredValue takes a value and returns a copy that intentionally lags during heavy renders, letting you show the previous result while the expensive part catches up. Neither makes work faster - they improve perceived responsiveness by prioritizing urgent input.",
        dia: "urgent: setQuery(text)  -> input stays snappy\nnon-urgent: startTransition(() => setResults(filter(text)))\n   React can interrupt the heavy render\n   isPending = true while it works\n\nuseDeferredValue(query) -> lagging copy\n   render stale list, swap when ready",
        code: "const [isPending, startTransition] = useTransition();\nfunction onChange(e) {\n  setQuery(e.target.value);                 // urgent\n  startTransition(() => setResults(filter(e.target.value))); // deferred\n}\n// consumer-side alternative when you only have a value:\nconst deferred = useDeferredValue(query);\nconst list = useMemo(() => filter(deferred), [deferred]);",
        use: "Search-as-you-type over large lists, tab switches that render heavy trees, filtering big tables - anywhere a state update would otherwise block the input.",
        adv: ["Keeps urgent input responsive during heavy renders", "Built-in pending state (useTransition) for UX feedback", "No debounce hacks needed", "Composes with Suspense"],
        dis: ["Does not reduce total work, only reorders it", "Easy to over-apply where a cheap render would do", "A stale-UI window can confuse if not signalled", "Only helps CPU-bound render cost, not slow network"],
        co: "Used in data-heavy dashboards and search UIs; a common React 18 interview topic for senior roles.",
        alt: "Debounce/throttle for input, web workers for truly heavy compute, server-side filtering/pagination to avoid the big render altogether.",
        vid: [{ t: "useTransition explained", q: "react usetransition usedeferredvalue explained" }, { t: "React concurrent features", q: "react 18 concurrent rendering explained" }, { t: "Responsive React UIs", q: "react useTransition search performance" }],
        iq: [
          { q: "useTransition vs useDeferredValue?", a: "Both deprioritize work. useTransition wraps a state update you control (you call startTransition) and gives isPending. useDeferredValue is for when you only receive a value (e.g. a prop) and want a lagging copy with no access to the setter. Use a transition at the source, a deferred value at the consumer." },
          { q: "Does useTransition make rendering faster?", a: "No. It lets React interrupt an expensive render to handle urgent updates like typing first, so the app feels responsive. Total work is the same or slightly more; the win is perceived performance and no input lag." },
        ],
      },
      "useSyncExternalStore": {
        def: "useSyncExternalStore is the hook for safely subscribing a component to an external (non-React) store - Redux, Zustand, a browser API, or any global event source. You pass a subscribe function and a getSnapshot function, and React guarantees the component reads a consistent value even under concurrent rendering, plus a third server-snapshot argument for SSR. It exists to fix tearing, where two parts of the UI could otherwise read different values of the same store mid-render.",
        dia: "external store (outside React)\n   subscribe(cb) -> unsubscribe\n   getSnapshot() -> current value\n        |\n        v\nuseSyncExternalStore keeps every consumer\nconsistent during a concurrent render (no tearing)\n   + getServerSnapshot() for SSR / hydration",
        code: "const value = useSyncExternalStore(\n  store.subscribe,             // (cb) => unsubscribe\n  () => store.getState(),      // client snapshot\n  () => store.getServerState() // optional SSR snapshot\n);",
        use: "Mostly when authoring a state library or integrating a browser API (online status, media query, location). Application code usually consumes a library that uses it internally.",
        adv: ["Prevents tearing under concurrent rendering", "Correct SSR/hydration via the server snapshot", "One standard way to wire any external source", "Avoids subtle useEffect subscription bugs"],
        dis: ["Low-level and verbose for app code", "getSnapshot must return a stable reference or it loops", "Rarely needed directly outside libraries"],
        co: "Used inside Redux, Zustand and similar libraries; appears in senior interviews as the 'how do libraries integrate with React safely' question.",
        alt: "A state library (Zustand/Redux/Jotai) that wraps it; useState+useEffect subscription for simple non-concurrent cases (with tearing risk).",
        vid: [{ t: "useSyncExternalStore explained", q: "react usesyncexternalstore explained" }, { t: "Tearing in React", q: "react concurrent tearing explained" }, { t: "Build a store hook", q: "usesyncexternalstore custom store tutorial" }],
        iq: [
          { q: "What problem does useSyncExternalStore solve?", a: "Tearing under concurrent rendering: without it an external store could change mid-render and different components read different values. It subscribes safely, forces a consistent snapshot, and handles SSR hydration via the server snapshot." },
          { q: "When would you call it directly?", a: "When building a state library or integrating a browser/external API. Most app code consumes a library (Zustand, Redux) that already uses it under the hood." },
        ],
      },
    } },
{ key: "angular", cat: "Frontend", name: "Angular", icon: "NG", hue: "#dd0031", docs: "https://angular.dev/overview",
    blurb: "Angular is a complete, opinionated framework from Google for building large-scale single-page apps. It ships everything in the box (routing, forms, HTTP, dependency injection, RxJS) and uses TypeScript and a component architecture. Modern Angular (standalone components, signals) makes it leaner while keeping enterprise-grade structure.",
    sections: [
      { t: "Angular Fundamentals", s: "Components and templates", d: "3 wks", k: ["Components and templates", "Data binding and interpolation", "Directives (ngIf, ngFor)", "Standalone components", "Pipes", "Lifecycle hooks"] },
      { t: "Services and DI", s: "App architecture", d: "2 wks", k: ["Services and dependency injection", "Modules vs standalone", "HttpClient", "Observables and RxJS", "Signals", "State management"] },
      { t: "Routing and Forms", s: "Navigation and input", d: "2-3 wks", k: ["Router and guards", "Lazy loading", "Template-driven forms", "Reactive forms", "Form validation", "Route resolvers"] },
      { t: "Advanced and Tooling", s: "Production Angular", d: "1 mo", k: ["Change detection and OnPush", "RxJS operators", "Angular CLI", "Testing (Karma/Jasmine)", "NgRx", "SSR (Angular Universal)"] },
    ], detail: {} },
{ key: "three", cat: "Frontend", name: "Three.js", icon: "3D", hue: "#ffb000", docs: "https://threejs.org/docs/",
    blurb: "Three.js is the most popular JavaScript library for 3D graphics in the browser, wrapping the low-level WebGL API in an approachable scene-graph model. You build a Scene, add Meshes (geometry plus material) and lights, point a Camera at it, and a Renderer draws it to a canvas every frame. It powers 3D product viewers, data viz, games and immersive experiences.",
    sections: [
      { t: "Fundamentals", s: "Scene, camera, renderer", d: "1-2 wks", k: ["Scene, Camera and Renderer", "Geometries and custom shapes", "Materials and textures", "Lighting and shadows", "Cameras and OrbitControls", "Animation loop"] },
      { t: "Assets and Animation", s: "Bringing scenes to life", d: "2-3 wks", k: ["Loading 3D models (glTF)", "Animation and tweening", "Skeletal animation", "Raycasting and interaction", "Textures and UV mapping", "Environment maps"] },
      { t: "Advanced Rendering", s: "Effects and performance", d: "2-3 wks", k: ["Postprocessing effects", "Custom shaders (GLSL)", "Performance optimization", "Instancing and LOD", "Physics (cannon/rapier)", "React Three Fiber"] },
    ],
    detail: { "Scene, Camera and Renderer": {
      def: "Three.js is built on three pillars. The Scene is a container: a graph of everything you want to display (meshes, lights, groups). The Camera defines the viewpoint and projection (a PerspectiveCamera mimics human vision with a field of view). The Renderer (usually WebGLRenderer) takes the scene and camera and draws the result to a canvas each frame. You repeatedly call renderer.render(scene, camera) inside requestAnimationFrame to produce live, animated 3D.",
      dia: "Scene (container)\n  +-- Mesh = Geometry + Material\n  +-- Light\n  +-- Group(...)\n       |   Camera (viewpoint, FOV)\n       v   v\n     Renderer (WebGL)\n       | renderer.render(scene, camera)\n       v\n     <canvas> (pixels on screen)",
      code: "import * as THREE from 'three';\nconst scene = new THREE.Scene();\nconst camera = new THREE.PerspectiveCamera(\n  75, innerWidth/innerHeight, 0.1, 1000);\ncamera.position.z = 5;\nconst renderer = new THREE.WebGLRenderer();\nrenderer.setSize(innerWidth, innerHeight);\nconst cube = new THREE.Mesh(\n  new THREE.BoxGeometry(1,1,1),\n  new THREE.MeshStandardMaterial({color:0xff7700}));\nscene.add(cube);\nfunction animate(){\n  requestAnimationFrame(animate);\n  cube.rotation.y += 0.01;\n  renderer.render(scene, camera);\n}\nanimate();",
      use: "3D product configurators (cars, shoes), data viz, browser games, portfolio sites, AR/VR on the web.",
      adv: ["Approachable API over raw WebGL", "Huge ecosystem and examples", "Cross-browser, GPU-accelerated", "Works with React via R3F"],
      dis: ["3D math learning curve", "About 150KB bundle", "Performance needs tuning", "Mobile GPUs struggle with heavy scenes"],
      co: "Nike, Ikea room planner, Spline, and countless agency and portfolio sites.",
      alt: "Babylon.js (more game-engine features), PlayCanvas, raw WebGL/WebGPU, Unity WebGL exports.",
      vid: [{ t: "Three.js crash course", q: "three.js crash course beginners" }, { t: "Three.js fundamentals", q: "three.js fundamentals scene camera renderer" }, { t: "Three.js Journey", q: "three.js journey tutorial bruno simon" }] , iq: [{"q":"What three objects do you need to render anything in Three.js?","a":"A Scene (holds objects and lights), a Camera (viewpoint and projection), and a Renderer (draws scene+camera to a canvas). You call renderer.render(scene, camera) every frame."},{"q":"Perspective vs Orthographic camera?","a":"PerspectiveCamera mimics the eye - objects shrink with distance (has a field of view), good for 3D. OrthographicCamera has no perspective distortion, good for 2D, isometric or CAD views."},{"q":"Why use requestAnimationFrame instead of setInterval?","a":"rAF syncs with the display refresh, pauses on hidden tabs, and avoids dropped or extra frames - smoother and more efficient than a fixed-interval timer."}] },
    "Loading 3D models (glTF)": {
      def: "Rather than building complex shapes by hand, you load artist-made models from files. glTF/GLB is the modern standard: a compact, web-optimised format that bundles geometry, materials, textures and animations together. Three.js loads it with GLTFLoader, which fetches the file asynchronously and gives you a scene object you add to your world, plus animation clips you can play. DRACO compression can shrink geometry to a fraction of its size for fast mobile loads.",
      dia: "Blender / Maya export\n  |  model.glb (geometry+materials+anim)\n  v\nGLTFLoader.load(url, onLoad, onProgress, onError)\n  | onLoad(gltf)\n  v\ngltf.scene      -> add to your Scene\ngltf.animations -> play via AnimationMixer",
      code: "import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';\nconst loader = new GLTFLoader();\nloader.load('/models/robot.glb', (gltf) => {\n  scene.add(gltf.scene);\n  const mixer = new THREE.AnimationMixer(gltf.scene);\n  if (gltf.animations.length)\n    mixer.clipAction(gltf.animations[0]).play();\n});",
      use: "3D product viewers, character models in web games, avatars, architectural walkthroughs, AR exhibits.",
      adv: ["glTF is the web-native 3D standard", "One file: mesh + materials + animation", "DRACO compression shrinks size", "Async loading with progress"],
      dis: ["Large models hurt load/memory", "Materials may differ from preview", "Needs a server (CORS) to load", "Animation rigs add complexity"],
      co: "Sketchfab, Shopify 3D product pages, Google Model Viewer use glTF.",
      alt: "OBJ+MTL (legacy, no animation), FBX (heavier), USDZ for Apple AR Quick Look.",
      vid: [{ t: "Load glTF in Three.js", q: "three.js gltf loader tutorial" }, { t: "Three.js model loading", q: "three.js load 3d model glb tutorial" }, { t: "glTF export tips", q: "blender gltf export three.js" }] , iq: [{"q":"Why is glTF preferred over OBJ or FBX on the web?","a":"glTF is web-first: compact, fast to parse, and bundles geometry, materials, animations and textures in one file. OBJ has no animation and is verbose; FBX is heavier and proprietary."},{"q":"GLTFLoader.load is asynchronous - how do you handle it?","a":"Use the onLoad/onProgress/onError callbacks (or a Promise wrapper): add the model in onLoad, show progress with onProgress, and serve files over http (CORS), not file protocol."},{"q":"What is DRACO and when do you use it?","a":"DRACO compresses mesh geometry and shrinks files a lot. Use it for big models on mobile or slow networks; it needs a decoder configured on the loader and adds a small decode cost."}] } } },
{ key: "frontend", cat: "Frontend", name: "Frontend Roadmap", icon: "FE", hue: "#f72585", docs: "https://roadmap.sh/frontend",
    blurb: "Frontend engineering is everything the user sees and touches in the browser. Beyond HTML/CSS/JS, a modern frontend developer owns accessibility, performance, responsive design, state management, build tooling and testing. This roadmap sequences the whole journey from markup to production-grade single-page apps.",
    sections: [
      { t: "The Web Platform", s: "HTML, CSS, JS core", d: "1-2 mo", k: ["Semantic HTML", "CSS box model and layout", "Flexbox and Grid", "Responsive design", "JavaScript fundamentals", "DOM and events"] },
      { t: "Modern CSS and Styling", s: "Scalable UI", d: "3 wks", k: ["CSS variables", "Tailwind / utility CSS", "Sass / CSS modules", "Animations and transitions", "Accessibility (a11y, ARIA)", "Design systems"] },
      { t: "JavaScript and Frameworks", s: "Interactivity at scale", d: "1-2 mo", k: ["ES6+ features", "Fetch and async/await", "A framework (React/Vue)", "State management", "Routing (SPA)", "TypeScript"] },
      { t: "Tooling and Quality", s: "Ship with confidence", d: "1 mo", k: ["npm / pnpm", "Bundlers (Vite/webpack)", "Git and GitHub", "Testing (Vitest/Playwright)", "Web performance (Core Web Vitals)", "Deployment (Vercel/Netlify)"] },
    ], detail: {} },
{ key: "rn", cat: "Frontend", name: "React Native", icon: "RN", hue: "#61dafb", docs: "https://reactnative.dev/docs/getting-started",
    blurb: "React Native lets you build genuinely native iOS and Android apps using React and JavaScript, sharing most of your code across platforms. Components map to real native widgets (not a webview), and the New Architecture brings near-native performance. With Expo, you can go from zero to a running app on your phone in minutes.",
    sections: [
      { t: "RN Fundamentals", s: "Core components and styling", d: "2-3 wks", k: ["Core components (View, Text, Image)", "Flexbox styling in RN", "Lists (FlatList, SectionList)", "Touchables and gestures", "Platform-specific code", "Expo vs bare workflow"] },
      { t: "Navigation and State", s: "App structure", d: "2 wks", k: ["React Navigation", "Stack/Tab/Drawer navigators", "Passing params", "State management", "AsyncStorage", "Context and hooks"] },
      { t: "Native Capabilities", s: "Device features", d: "3 wks", k: ["Camera and media", "Permissions", "Push notifications", "Geolocation and maps", "Native modules", "Animations (Reanimated)"] },
      { t: "Ship It", s: "Build and release", d: "2 wks", k: ["Debugging", "Performance optimization", "EAS Build", "App Store / Play Store release", "OTA updates", "Testing"] },
    ], detail: {} },
{ key: "flutter", cat: "Frontend", name: "Flutter", icon: "FL", hue: "#02a8f4", docs: "https://docs.flutter.dev/",
    blurb: "Flutter is Google's UI toolkit for building natively-compiled apps for mobile, web, desktop and embedded from a single Dart codebase. Instead of using platform widgets, Flutter draws every pixel itself with its rendering engine, giving pixel-perfect consistency and high-framerate performance. Everything in Flutter is a widget.",
    sections: [
      { t: "Dart and Widgets", s: "The Flutter mental model", d: "3 wks", k: ["Dart language basics", "Everything is a widget", "Stateless vs Stateful widgets", "Layout widgets (Row, Column, Stack)", "Material and Cupertino", "Hot reload"] },
      { t: "State Management", s: "Managing app data", d: "2-3 wks", k: ["setState", "InheritedWidget", "Provider", "Riverpod", "Bloc / Cubit", "Navigation and routing"] },
      { t: "Data and Backend", s: "Connecting to the world", d: "2 wks", k: ["HTTP and REST", "JSON serialization", "Firebase integration", "Local storage (Hive/SQLite)", "Async and Futures/Streams", "Forms and validation"] },
      { t: "Polish and Release", s: "Production Flutter", d: "2 wks", k: ["Animations", "Responsive and adaptive UI", "Testing (widget/integration)", "Performance profiling", "Build for iOS/Android", "Publishing"] },
    ], detail: {} },
];
